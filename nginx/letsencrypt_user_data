# DEPRECATED / UNUSED.
# Beachhead no longer bind-mounts this file. The acme-companion standalone-cert
# list is now delivered directly into the acme container via `docker cp`
# (see src/services/certs.js). This file is kept only to avoid a dangling
# reference and can be safely deleted.
